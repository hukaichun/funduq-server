# Only pyproject/uv.lock/.python-version, souk_server/ and souk-agent-sdk/
# are COPYed (see Dockerfile) — this trims the build context rather than
# the image.
.git
.venv
__pycache__
*.pyc
.pytest_cache
.env
*.db
*.db-wal
*.db-shm
*.pem
*.key
